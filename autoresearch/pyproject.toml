[project]
name = "autoresearch"
version = "0.2.0"
description = "Self-contained species-level CDS optimization with small Transformer, local reward model, and evolutionary search"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "pandas>=2.3.3",
    "torch>=2.2.2",
]
