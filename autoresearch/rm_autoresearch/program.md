# RM Autoresearch Program

This unit follows the original autoresearch contract:

- `program.md` is human-owned
- `prepare.py` is fixed
- `train.py` is the only file the agent edits

The runtime worker may replace this file inside an isolated git worktree for one RM stage run.
